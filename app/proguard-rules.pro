# Add project specific ProGuard rules here.
# This app doesn't currently enable minification (isMinifyEnabled = false
# in build.gradle.kts), so this file isn't active yet - kept as a
# placeholder so the build.gradle.kts reference to it doesn't break if
# minification is turned on later.
